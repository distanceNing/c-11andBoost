#include <iostream>
#include <functional>

using namespace std;

int main()
{
	//inline function
	//lambda 可以函数对象

	auto print=[] {
		cout << "hello lambdas!" << endl;
	};
	print();

	//因为在lambda中引用到了fib 所以必须显式的给出fib的类型	
	
	function<int(int)> fib = [&fib](int arg) {
		return arg < 2 ? 1 : fib(arg - 1) + fib(arg - 2);
	};

	int res = 0;
	auto fib2 = [&res](int arg) {
		while (arg > 1)
		{
			
		}
	};
	
	cout << decltype(fib) << endl;
	getchar();
	return 0;
}